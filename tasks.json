{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmpl",
            "type": "shell",
            "command": "G++ \"${fileDirname}\\${fileBasename}\" -o \"${fileDirname}\\${fileBasenameNoExtension}\" -g -std=c++11 -Wall -Wextra -Wconversion",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}